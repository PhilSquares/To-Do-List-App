<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
</head>
<body>
    <!-- Passing information from app.js to the <h1> tag below using a marker. -->
    <!-- Scriplet Tag ("<%  %>"): Add around any tag that is NOT HTML (JavaScript specifically) -->
        <% if(kindOfDay === "Saturday" || kindOfDay === "Sunday"){ %>
            <h1 style="color: purple"><%= kindOfDay %> To-Do List</h1>
        <% } else { %>
            <h1 style="color: blue"><%= kindOfDay %> To-Do List</h1>
        <% } %>
</body>
</html>